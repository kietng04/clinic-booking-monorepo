-- This SQL file is loaded before tests to handle H2 compatibility issues
-- Note: H2 treats 'value' as a reserved keyword, so we need to quote it

-- Health metrics table will be created by Hibernate with quoted column names if needed
